[project]
name = "w10-federated-learning-security-experiment"
version = "0.1.0"
description = "Week 10 AI security experiment environment"
requires-python = ">=3.11,<3.12"
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn",
    "matplotlib",
    "pyyaml",
]

[tool.uv]
package = false
