# W03 데이터 폴더

본 실습은 `src/run_experiment.py`에서 synthetic 8x8 bar image를 런타임에 생성한다. 별도 원본 데이터 파일을 이 폴더에 저장하지 않는다.

## 원칙

- 실제 개인정보, 운영 서비스 이미지, 무단 수집 데이터를 저장하지 않는다.
- 외부 데이터셋을 추가할 경우 출처, 라이선스, 다운로드 일자, 해시를 함께 기록한다.
- 현재 제출 수치는 synthetic 데이터와 seed 42 조건의 실행 로그만 기준으로 한다.
