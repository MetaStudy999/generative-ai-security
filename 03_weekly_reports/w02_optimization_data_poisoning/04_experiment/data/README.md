# W02 데이터 폴더

본 실습은 scikit-learn 내장 공개 digits 데이터셋을 런타임에 불러온다. 별도 원본 데이터 파일을 이 폴더에 저장하지 않는다.

## 원칙

- 실제 개인정보 또는 운영 로그를 저장하지 않는다.
- 외부 데이터셋을 추가할 경우 출처, 라이선스, 다운로드 일자, 해시를 이 폴더에 함께 기록한다.
- 현재 실습 기준 데이터 로더는 `src/run_experiment.py`의 `load_digits()` 호출이다.
