[project]
name = "w12-nn-verification-xai-experiment"
version = "0.1.0"
description = "Week 12 AI security experiment environment"
requires-python = ">=3.11,<3.12"
dependencies = [
    "pyyaml",
]

[tool.uv]
package = false
